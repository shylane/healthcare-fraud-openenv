# AgentBeats Leaderboard Submission
# Submit by pushing this file after filling in agent IDs.
# Or use Quick Submit on the AgentBeats leaderboard page.

[green_agent]
# agentbeats_id: UUID assigned after registration on agentbeats.dev
agentbeats_id = "019d84d9-6ee6-70b0-9a21-49338626e967"
# Fully qualified public image — used for local `agentbeats run` and leaderboard execution
image = "ghcr.io/shylane/healthcare-fraud-openenv-evaluator:latest"
# env = { HF_TOKEN = "${HF_TOKEN}" }   # uncomment if env data is gated

[[participants]]
# agentbeats_id: UUID assigned to the purple agent after their registration
agentbeats_id = ""
# Fully qualified public image of the purple agent being evaluated
image = ""
name = "healthcare-fraud-rl-agent"
# env = { HF_TOKEN = "${HF_TOKEN}" }

[config]
# Parameters aligned with the evaluation study (blog/hf_blog_post.md):
# 20 episodes × 100 claims × 5% fraud rate, seed=42.
# Previous placeholder used 5 eps / 20 claims / 15% fraud — wrong on all three.
num_episodes = 20
claims_per_episode = 100
fraud_rate = 0.05
seed = 42
